<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="web.com.springweb.a00_config.aop.ExceptionLogDao">

	<!-- 예외 로그 리스트 조회 -->
    <select id="getExceptionLogList" parameterType="web.com.springweb.vo.ExceptionLogSearchDto"  resultType="web.com.springweb.vo.ExceptionLog">
        SELECT *
        FROM (
            SELECT ROWNUM rn, a.*
            FROM (
                SELECT * 
                FROM EXCEPTION_LOG
                WHERE CLASS_NAME LIKE '%' || #{keyword} || '%'
                   OR METHOD_NAME LIKE '%' || #{keyword} || '%'
                   OR EXCEPTION_MESSAGE LIKE '%' || #{keyword} || '%'
                ORDER BY OCCURRED_AT DESC
            ) a
            WHERE ROWNUM &lt;= #{end}
        )
        WHERE rn &gt;= #{start}
    </select>

    <!-- 총 개수 조회 -->
    <select id="getTotalCount" parameterType="web.com.springweb.vo.ExceptionLogSearchDto"  resultType="int">
        SELECT COUNT(*)
        FROM EXCEPTION_LOG
        WHERE CLASS_NAME LIKE '%' || #{keyword} || '%'
           OR METHOD_NAME LIKE '%' || #{keyword} || '%'
           OR EXCEPTION_MESSAGE LIKE '%' || #{keyword} || '%'
    </select>

</mapper>